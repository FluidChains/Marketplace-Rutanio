<div class="container">
  <div class="row">
    <div class="col s12 m12 l12">
      <p id="notice"><%= notice %></p>

      <h1 class="fucsia_font">Mis Publicaciones</h1>

      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Information</th>
            <th>Skills</th>
            <th>Precio</th>
            <th>Valid until</th>
            <th>Aditional info</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @services.each do |service| %>
            <tr>
              <td><%= service.name %></td>
              <td><%= service.information %></td>
              <td><%= service.skills %></td>
              <td><%= service.precio %></td>
              <td><%= service.valid_until %></td>
              <td><%= service.aditional_info %></td>
              <td><%= link_to 'Show', service %></td>
              <td><%= link_to 'Edit', edit_service_path(service) %></td>
              <td><%= link_to 'Destroy', service, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>

      <%= link_to 'New Service', new_service_path %>


    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <% @services.each do |service| %>

      <div class="col s12 m6 l4">
          <%= link_to service  do  %>
        <div class="card">
          <%= link_to 'Editar', edit_service_path(service) %>
          <%= link_to 'Eliminar', service, method: :delete, data: { confirm: 'Are you sure?' } %>

          <div class="card-content black-text">
            <span class="card-title "><%= service.name  %></span>
            <p><%= service.information.truncate(150)  %> </p>
            <div class="container">
              <div class="row">
                <i class="material-icons">update</i>
                <%= service.valid_until.strftime("%d-%m-%Y ")  %>
                <span>vencimiento</span>
              </div>
              <div class="row">
                <a href="#"><img src="images/ruta.png" alt=""  class="iconos_market"> <%= number_with_delimiter(service.precio.round, :delimiter => ',') %></a>
                <span>Rutas</span>
              </div>
              <div class="row">
                <a href="#"><img src="images/exos.png" alt=""  class="iconos_market"> <%= number_with_delimiter(service.precio.round, :delimiter => ',') %></a>
                <span>Rutas</span>
              </div>
            </div>
          </div>
          <div class="card-action">

            <div class="col s3 m3 l3 bage ">
              <span class="new badge morado_ruta">Desarrollador</span>
            </div>
            <div class="col s3 m3 l3 bage">
              <span class="new badge morado_ruta">Desarrollador</span>
            </div>
            <div class="col s3 m3 l3 bage">
              <span class="new badge morado_ruta">Desarrollador</span>
            </div>
            <div class="col s3 m3 l3 bage">
              <span class="new badge morado_ruta">Desarrollador</span>
            </div>
            <div class="col s3 m3 l3 bage">
              <span class="new badge morado_ruta">Desarrollador</span>
            </div>


          </div>
          <br>


        </div>
             <% end %>
      </div>


    <% end %>


  </div>
</div>
