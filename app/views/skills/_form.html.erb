<div class="container">
  <%= form_with(model: skill, local: true) do |form| %>
    <% if skill.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

        <ul>
        <% skill.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>
    <div class="input-field col s12 m6 l6 center">
      <br><br>
      <%= form.label :color_tag %>
      <%= form.color_field :color_tag, class:"col s6 m6 l6" %>
    </div>


    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>


</div>
